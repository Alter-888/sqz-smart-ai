# 改造前基线（P-1）

> 用途：改造多智能体前的「改造前」数字。没有它，后面所有「提升了多少」都无从谈起。
> 数据来源：MySQL `sqz-smart-ai.ai_chat_turn_audit`（RuoYi 单智能体客服「小智」）。
> 生成日期：2026-09-06

## 一、官方基线（05 §1.1，文档定稿时 82 轮）

| 指标 | 值 | 口径 |
|---|---|---|
| 失败率 | **6.1%** | 5 / 82 轮回复为「系统暂时繁忙」（重试耗尽） |
| 耗时中位数 | **10.7 秒** | duration_ms 中位数 |
| p95 延迟 | **约 29 秒** | duration_ms 95 分位 |
| 最慢一轮 | **96.9 秒** | duration_ms 最大值 |

> 这是 `05-指标与评测设计.md` §1.1 抄下来的权威基线：失败率 6.1% 已超过 5% 预警线；p95 29 秒对客服对话偏高。这是整套改造的起点。

## 二、当前重算基线（2026-09-06 最新，98 轮）

> ⚠️ 注意：审计表已增长到 98 行，其中含最近一批（session 40）因阿里云百炼**免费额度耗尽 403** 造成的「系统暂时繁忙」。这批属配额故障、非系统正常行为，会抬高失败率；中位/p95 主要反映真实推理耗时，相对可信。

### 2.1 全体（98 轮，含配额故障）

| 指标 | 值 |
|---|---|
| 总轮次 | 98 |
| 失败（「系统繁忙」） | 11 |
| 失败率 | 11.22% |
| 平均耗时 | 13.29 秒 |
| 中位耗时 | 11.07 秒 |
| p95 延迟 | 29.05 秒 |
| 最慢一轮 | 96.94 秒 |
| RAG 命中率 | 37.76% |

### 2.2 健康轮次（剔除「系统繁忙」，87 轮）

| 指标 | 值 |
|---|---|
| 轮次 | 87 |
| 平均耗时 | 13.15 秒 |
| 中位耗时 | 10.55 秒 |
| p95 延迟 | 16.64 秒 |
| 最慢一轮 | 96.94 秒 |

### 2.3 工具调用分布（越频繁 = 模型越依赖该能力）

| 工具 | 次数 |
|---|---|
| searchProducts | 42 |
| recommendProducts | 16 |
| queryOrder | 12 |
| viewCart | 6 |
| queryUserOrders | 5 |
| addToCart | 3 |
| escalateToHuman | 2 |
| queryUserInfo | 2 |
| queryUserAddresses | 2 |
| checkoutFromCart | 2 |
| queryUnreadNotifications | 1 |
| getProductDetail | 1 |
| addAddress | 1 |

## 三、口径说明

- 指标定义见 `05-指标与评测设计.md` §1.1。
- 改造前检索：`SimpleVectorStore` + topK=3 + threshold=0.5（05 §7.1）。
- 模型：存量审计数据为改造前 `qwen3.5-plus-2026-02-15` 生成（现在 8080 仍是旧模型）；后续统一改用 `qwen3.7-flash`（方案 A）。前后对比时需注意模型差异并非多智能体结构引入。